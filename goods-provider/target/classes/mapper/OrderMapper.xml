<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="GoodsProvider.Dao.OrderDao">
    <select id="getAllorder" resultType="model.Userorder" parameterType="int">
        select * from userorder WHERE uid = #{uid}
    </select>
</mapper>